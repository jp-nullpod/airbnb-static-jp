<h1>Flats</h1>

<ul>
    <% @flats.each do |flat| %>
        <li>
            <h3><%= flat["name"] %></h3>
            <p> 💰 <%= flat["price"] %> <%= flat["priceCurrency"] %>
            <%= image_tag flat["imageUrl"], style: 'height:100px;width:auto;' %>
        </li>
    <% end %>
</ul>


